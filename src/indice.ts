const edad: number = 30; 

// edad = "30"; // error porque no se puede asignar un string a un number



//new Map(); // es un objeto que nos permite almacenar valores de cualquier tipo, incluso objetos o funciones. 


// vamos a tocar cuando traemos elementos en variables como verificar si es nulo o no, ahi tenemos “strictNullChecks” que es una propiedad que nos permite hacer eso. 

// tambien podemos hacer esto con el signo ! o con una condicion if 

let element = document.getElementById("element"); 

if(element != null) {
    console.log(element.innerHTML);
}

// tambien podemos hacer esto con el signo ! luego de la varible let element = document.getElementById("element")!;


